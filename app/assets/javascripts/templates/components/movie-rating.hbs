<div>
  <p>Movie Rating Component</p>
  <p>Your score: {{ score }}</p>

  {{#each rating.score_array as |index|}}
    <input type="radio" name='score' value={{index}}>{{index}}<br>
  {{/each}}

</div>
